{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "NSIPTimelineEntry",
  "type": "object",
  "description": "TODO: Placeholder schema for a NSIP timeline entry.",
  "properties": {
    "nsip_timeline_entry_id": { "type": "string", "format": "uuid" },
    "nsip_case_id": { "type": "string", "format": "uuid" },
    "event_type": { "type": "string" },
    "summary": { "type": "string" },
    "occurred_at": { "type": "string", "format": "date-time" },
    "evidence_ref_ids": {
      "type": "array",
      "items": { "type": "string", "format": "uuid" }
    }
  },
  "required": ["nsip_timeline_entry_id", "nsip_case_id", "event_type", "occurred_at"]
}
